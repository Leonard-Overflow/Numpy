import numpy as np

array = np.arange(10)

# array[comeco(inclusivo):fim(exclusivo):passo]

# Exemplos
print(array[2:8:2]) # numeros pares entre 1 e 8
print(array[::-1]) # array invertido
print(array[::-2]) # impares invertidos